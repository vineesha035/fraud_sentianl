# Financial Fraud Detection at Scale — Python dependencies
# For local/dev: use with Spark 3.x+ or run notebook on Databricks

# Core data & Spark
pyspark>=3.4.0
pandas>=1.5.0
numpy>=1.23.0
pyarrow>=12.0.0

# ML
scikit-learn>=1.2.0
xgboost>=2.0.0

# Visualization
matplotlib>=3.6.0
seaborn>=0.12.0

# MLOps & experiment tracking
mlflow>=2.9.0

# Optional: for config
pyyaml>=6.0

# Optional: for local notebook (if not using Databricks)
jupyter>=1.0.0
ipykernel>=6.0.0
